#include "ScoreTracker.h"

ScoreTracker::ScoreTracker() : m_board(2)
{
}

ScoreTracker::~ScoreTracker()
{
}

std::pair<int, int> ScoreTracker::evaluateScore(int indexPlayer, int move)
{
	return std::pair<int, int> (0,0);
}
